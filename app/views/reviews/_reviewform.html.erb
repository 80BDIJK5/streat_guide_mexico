 <%= simple_form_for [ @stall, @review ] do |f| %>
    <%= f.input :title %>
    <%= f.input :comment %>
    <%=f.input :rating, input_html: { type: 'number' } %>
    <%= link_to "Submit a review", stall_path(@stall), class: 'btn btn-block btn-primary' %>
  <% end %>
